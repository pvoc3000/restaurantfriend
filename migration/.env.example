# Copy to `migration/.env` and fill in. NEVER commit the real file — the
# service_role key bypasses RLS entirely. It is for local scripts in this
# folder only and must never appear in web/.
#
# Supabase Studio → Settings → API → Project API keys → service_role (Reveal)

SUPABASE_URL=https://kltxioacvneshbyhxtaj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
